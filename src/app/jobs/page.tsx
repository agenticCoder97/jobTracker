import { PlaceholderApp } from '@/components/jobtracker/JobTrackerApp';

export default function JobsPage() {
  return (
    <PlaceholderApp
      title="Jobs ships in Plan 2"
      description="Plan 2 implements the Jobs table, URL-backed filters, job listing preview modal, and wishlist creation from untracked listings."
    />
  );
}
