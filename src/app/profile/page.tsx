import { PlaceholderApp } from '@/components/jobtracker/JobTrackerApp';

export default function ProfilePage() {
  return (
    <PlaceholderApp
      title="Profile ships in Plan 3"
      description="Plan 3 implements the profile hero, five tabs, editable About, resume and cover-letter managers, side cards, and reset demo data."
    />
  );
}
